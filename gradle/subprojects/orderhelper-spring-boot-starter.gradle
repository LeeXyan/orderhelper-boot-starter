def springBootGroup = "org.springframework.boot"

dependencies {
    compile "${springBootGroup}:spring-boot-starter:${springBootVersion}"
    compile "${project.group}:orderhelper-spring-boot-autoconfigure:1.0"
    compile "tk.mybatis:orderby-helper:${orderVersion}"
}